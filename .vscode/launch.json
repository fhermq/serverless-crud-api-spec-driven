{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "SAM CLI - Debug Create Item",
      "type": "aws-sam",
      "request": "direct-invoke",
      "invokeTarget": {
        "target": "template",
        "templatePath": "${workspaceFolder}/infrastructure/template.yaml",
        "logicalId": "CreateItemFunction"
      }
    }
  ]
}
